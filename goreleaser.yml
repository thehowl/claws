build:
  goos:
    - windows
    - darwin
    - linux
archive:
  replacements:
    amd64: 64bit
    386: 32bit
  format_overrides:
    - goos: windows
      format: zip
snapshot:
  name_template: devel
release:
  draft: true
